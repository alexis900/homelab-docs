---
rfc: RFC-2026-0011-DOC
titulo: Documentación del servidor PXE
tipo: DOC
estado: Completado
criticidad: Baja
fecha: 2026-02-12
fecha_resolucion: 2026-02-12
autor: Alejandro Martín Pérez
---

# RFC — Documentación del servidor PXE

## Resumen

Se documenta el rol y las pautas operativas del servidor `pxe.home.arpa` (10.0.30.20) para que la entrega de arranque por red quede registrada en la documentación oficial y el histórico de cambios.

## Motivación

- El servicio ya figura en el inventario (`Doc-Red/Servicios.md`) pero faltaban detalles operativos y referencias formales para el equipo.  
- Contar con una RFC específica facilita el seguimiento de futuras modificaciones o ampliaciones del servidor PXE y asegura que el inventario DNS y de servicios queden alineados.  
- Reducir el riesgo de perder contexto cuando se cambia la imagen de arranque o cuando otros equipos necesiten usar este nodo como referencia.

## Alcance / Sistemas afectados

- `pxe.home.arpa` (10.0.30.20) en VLAN 30, que depende de la configuración de DHCP específica de PXE que mantiene OPNsense 26.1 mediante su plugin legacy.  
- Archivos de documentación: `Doc-Red/Servicios.md`, `Doc-Red/Cambios.md` y el índice concatenado `Doc-Red-full.md`.  
- Los procedimientos adyacentes donde se describan imágenes o scripts de arranque (por ejemplo SOPs futuros que hagan referencia a este servidor).

## Plan de acción

1. Registrar en `Doc-Red/Servicios.md` la sección detallada del servidor PXE con la IP, VLAN, operaciones y validaciones asociadas.  
2. Añadir el nuevo RFC en la tabla de RFCs completadas y en la sección de cambios recientes (`Doc-Red/Cambios.md`).  
3. Regenerar `Doc-Red-full.md` con `scripts/build-doc-red.sh` para mantener sincronizado el índice completo.  
4. Validar que la entrada DNS (`pxe.home.arpa`) sigue apuntando a 10.0.30.20 y que la operación de arranque por red funciona como se documentó.

## Riesgos

- La documentación puede quedar desactualizada si no se vinculan las adaptaciones de imágenes (`pxelinux.cfg`, initrds, kernels) con esta sección.  
- Cambios en la IP o VLAN podrían pasar desapercibidos si los responsables no actualizan simultáneamente `Doc-Red/Servicios.md` y el inventario DNS.  
- Si se regenera `Doc-Red-full.md` sin ejecutar el script oficial, el índice completo podría desincronizarse.

## Mitigación

- Usar siempre `scripts/build-doc-red.sh` tras editar los apartados de `Doc-Red` para garantizar que el archivo consolidado refleja el estado actual.  
- Documentar en esta sección cualquier ajuste a las imágenes de arranque y enlazarlo con una RFC (la presente o una futura) para dejar rastro.  
- Verificar la entrada DNS con `dig pxe.home.arpa @ns1` y mantener una pauta de validación post-cambio para el hardware que se arranque vía PXE.

## Plan de rollback

1. Revertir la edición de `Doc-Red/Servicios.md`, `Doc-Red/Cambios.md` y `Doc-Red-full.md` si se detecta información errónea.  
2. Volver a ejecutar `scripts/build-doc-red.sh` después de la reversión para regenerar el índice.  
3. Restituir esta RFC en el historial (mantenerla abierta) hasta que se confirme que la documentación correcta está operativa.

## Validaciones post-cambio

- [x] Nueva sección del servidor PXE presente en `Doc-Red/Servicios.md`.  
- [x] La tabla de RFCs completadas y la lista de cambios recientes mencionan `RFC-2026-0011-DOC`.  
- [x] `Doc-Red-full.md` reconstruido con todos los cambios de documentación.  
- [x] `dig pxe.home.arpa @ns1` resuelve a 10.0.30.20 (o la herramienta equivalente empleada por el equipo).

## Cambios DNS (si aplica)

- Entrada: `pxe.home.arpa` `A` `10.0.30.20` (motivo: Inventario/Documentación actualizados).  
- Validación: `dig pxe.home.arpa @ns1` / `ping 10.0.30.20`.

## Resultado (completar al cerrar)

- Se ha añadido la sección de servidor PXE al índice de servicios con instrucciones de operación y validación.  
- El RFC aparece reflejado en `Doc-Red/Cambios.md` y, al regenerar `Doc-Red-full.md`, el índice completo refleja el cambio.  
- La entrada DNS se mantiene apuntando a 10.0.30.20 y el inventario de VLANs confirma que sigue en VLAN 30.

## Observaciones

- Conviene enlazar esta documentación con SOPs o RFCs futuras que describan la gestión de imágenes (kernels, initrds) y la creación de nuevos targets de arranque.  
- Si en el futuro el PXE se traslada a otro host o VLAN, actualizar este RFC antes de cerrar el cambio para mantener trazabilidad.

## Firma

**Alejandro Martín Pérez** — 2026-02-12
